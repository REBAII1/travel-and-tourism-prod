{% extends 'base.html.twig' %}

{% block title %}Edit Logement{% endblock %}

{% block body %}
    <h2 class="text-center m-4">Edit Logement</h2>

    {{ include('logement/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="d-flex justify-content-center gap-3">
        <a href="{{ path('app_logement_index') }}" class="btn btn-primary">Back to list</a>
        
        {{ include('logement/_delete_form.html.twig') }}
    </div>
{% endblock %}
